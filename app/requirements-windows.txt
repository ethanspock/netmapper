networkx>=3.2
numpy==2.1.3
matplotlib==3.9.2
kiwisolver==1.4.5
pillow==10.4.0
psutil>=5.9
scapy>=2.5.0

# System tools to install separately on Windows:
# - Npcap (WinPcap-compatible API) for packet capture
# - Nmap (optional, for port scanning)
